# Copy to .env.local and fill in real values (ask the Zuper team).

# Used by configs/index.ts for the USER_DETAILS_WEBHOOK call
NEXT_PUBLIC_ZUPER_API_KEY=

# Google Maps JS API key for the address autocomplete in step-one
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
